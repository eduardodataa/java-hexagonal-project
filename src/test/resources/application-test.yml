spring:
  profiles:
    active: test
  
  cloud:
    aws:
      region:
        static: us-east-1
      credentials:
        access-key: test
        secret-key: test
      sqs:
        queue-name: test-debit-events
        command-queue-name: test-debit-commands

logging:
  level:
    com.example.hexagonal: DEBUG
    org.springframework.cloud.aws: DEBUG
